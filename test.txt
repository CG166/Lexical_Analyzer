if (a >= b)
{
    return 0; //Added comment for testing purposes
} //check if a is greater than or equal to b

int main(void){
    /*Variables*/
    string filename;
    string content;
    char c;
    bool iscomment = false;

    /*Getting filename from user*/
    cout << "Filename: ";
    getline(cin, filename);

    return 0;
}
